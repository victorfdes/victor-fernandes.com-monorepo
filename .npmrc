# Fail fast when the local Node/pnpm versions don't satisfy package.json "engines".
engine-strict=true

# Keep a single, deduplicated copy of peer dependencies shared by multiple dependents.
dedupe-peer-dependents=true

# Emit npm provenance attestations when publishing from CI (no-op for local installs).
provenance=true
